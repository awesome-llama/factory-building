costumes "costumes/blank.svg" as "blank";
hide;

onkey "1" { resolution = resolution_options[1+1]; }
onkey "2" { resolution = resolution_options[1+2]; }
onkey "3" { resolution = resolution_options[1+3]; }
onkey "4" { resolution = resolution_options[1+4]; }
onkey "5" { resolution = resolution_options[1+5]; }
onkey "6" { resolution = resolution_options[1+6]; }
onkey "7" { resolution = resolution_options[1+7]; }
onkey "8" { resolution = resolution_options[1+8]; }
onkey "9" { resolution = resolution_options[1+9]; }
onkey "0" { resolution = resolution_options[1+0]; }